window.SPLUNK_DATA = {

  events: [
    { _time:"2025-09-09 09:09:09", host:"artifacts.qayssarayra.com", status:200, uri:"/artifacts/flag_3.png",        src_ip:"10.9.3.1",   bytes:93939, user:"qays9",     method:"GET",  sourcetype:"access_combined", action:"success",  session:"sess_9a" },
    { _time:"2025-09-09 09:09:33", host:"ctfs.fun",                  status:200, uri:"/challenges/pwn/q9ctf",        src_ip:"10.9.3.3",   bytes:33339, user:"hidden",    method:"GET",  sourcetype:"access_combined", action:"success",  session:"sess_3b" },
    { _time:"2025-09-09 09:33:00", host:"qayssarayra.com",           status:404, uri:"/secret/9.html",               src_ip:"192.168.3.9",bytes:999,   user:"-",         method:"GET",  sourcetype:"access_combined", action:"not_found",session:"sess_1c" },
    { _time:"2025-09-09 09:39:03", host:"cryptera.ctfs.fun",         status:200, uri:"/crypto/aes_hidden_9",         src_ip:"10.9.0.3",   bytes:9393,  user:"qays9",     method:"POST", sourcetype:"access_combined", action:"success",  session:"sess_9a" },
    { _time:"2025-09-09 09:39:39", host:"osamaismail.com",           status:200, uri:"/blog/splunk-notes",           src_ip:"172.16.9.9", bytes:12393, user:"osama",     method:"GET",  sourcetype:"access_combined", action:"success",  session:"sess_5e" },
    { _time:"2025-09-09 09:33:03", host:"wadeehaddad.com",           status:301, uri:"/redirect/9",                  src_ip:"10.3.9.1",   bytes:339,   user:"-",         method:"GET",  sourcetype:"access_combined", action:"redirect", session:"sess_6f" },
    { _time:"2025-09-09 09:33:33", host:"qays.fun",                  status:200, uri:"/",                            src_ip:"10.9.3.9",   bytes:39993, user:"qays9",     method:"GET",  sourcetype:"access_combined", action:"success",  session:"sess_9a" },
    { _time:"2025-09-09 09:39:09", host:"artifacts.qayssarayra.com", status:500, uri:"/artifacts/upload",            src_ip:"10.3.3.9",   bytes:399,   user:"unknown",   method:"POST", sourcetype:"access_combined", action:"error",    session:"sess_8h" },
    { _time:"2025-09-09 09:03:09", host:"ctfs.fun",                  status:403, uri:"/admin/panel",                 src_ip:"172.16.3.9", bytes:993,   user:"attacker",  method:"GET",  sourcetype:"access_combined", action:"forbidden",session:"sess_9i" },
    { _time:"2025-09-09 09:03:33", host:"qayssarayra.com",           status:200, uri:"/projects/splunk",             src_ip:"10.9.9.9",   bytes:93939, user:"qays9",     method:"GET",  sourcetype:"access_combined", action:"success",  session:"sess_9a" },
    { _time:"2025-09-09 09:09:03", host:"cryptera.ctfs.fun",         status:200, uri:"/ctf/hidden_flag",             src_ip:"10.0.9.3",   bytes:9999,  user:"hidden",    method:"POST", sourcetype:"access_combined", action:"success",  session:"sess_3b" },
    { _time:"2025-09-09 09:39:00", host:"qays.fun",                  status:200, uri:"/about#9",                     src_ip:"192.168.9.9",bytes:33993, user:"qays9",     method:"GET",  sourcetype:"access_combined", action:"success",  session:"sess_9a" },
    { _time:"2025-09-09 09:33:09", host:"qayssarayra.com",           status:200, uri:"/ctf/write-up-3",              src_ip:"10.9.3.3",   bytes:39339, user:"qays9",     method:"GET",  sourcetype:"access_combined", action:"success",  session:"sess_9a" },
    { _time:"2025-09-09 09:03:00", host:"osamaismail.com",           status:404, uri:"/missing/page",                src_ip:"10.3.3.3",   bytes:339,   user:"-",         method:"GET",  sourcetype:"access_combined", action:"not_found",session:"sess_ne" },
    { _time:"2025-09-09 09:00:09", host:"wadeehaddad.com",           status:200, uri:"/portfolio/dev",               src_ip:"10.0.3.9",   bytes:93393, user:"wadee",     method:"GET",  sourcetype:"access_combined", action:"success",  session:"sess_ff" },
    { _time:"2025-09-09 09:09:39", host:"ctfs.fun",                  status:200, uri:"/scoreboard",                  src_ip:"10.9.9.3",   bytes:99399, user:"qays9",     method:"GET",  sourcetype:"access_combined", action:"success",  session:"sess_9a" },
    { _time:"2025-09-09 09:39:33", host:"qayssarayra.com",           status:200, uri:"/tools/9tool",                 src_ip:"10.3.9.9",   bytes:33993, user:"qays9",     method:"GET",  sourcetype:"access_combined", action:"success",  session:"sess_9a" },
    { _time:"2025-09-09 09:03:39", host:"artifacts.qayssarayra.com", status:200, uri:"/artifacts/qays9_badge.svg",   src_ip:"10.9.0.9",   bytes:3993,  user:"qays9",     method:"GET",  sourcetype:"access_combined", action:"success",  session:"sess_9a" },
    { _time:"2025-09-09 09:30:09", host:"cryptera.ctfs.fun",         status:200, uri:"/verify/flag_9",               src_ip:"192.168.9.3",bytes:9933,  user:"hidden",    method:"POST", sourcetype:"access_combined", action:"success",  session:"sess_3b" },
    { _time:"2025-09-09 09:30:39", host:"qays.fun",                  status:200, uri:"/challenges/3",                src_ip:"10.9.3.0",   bytes:39993, user:"qays9",     method:"GET",  sourcetype:"access_combined", action:"success",  session:"sess_9a" },
    { _time:"2025-09-09 09:33:39", host:"ctfs.fun",                  status:200, uri:"/ctf/web/sqli_9",              src_ip:"10.3.0.9",   bytes:93939, user:"qays9",     method:"POST", sourcetype:"access_combined", action:"success",  session:"sess_9a" },
    { _time:"2025-09-09 09:39:33", host:"qayssarayra.com",           status:200, uri:"/resume",                      src_ip:"172.16.3.3", bytes:93999, user:"recruiter", method:"GET",  sourcetype:"access_combined", action:"success",  session:"sess_rr" },
    { _time:"2025-09-09 09:09:33", host:"osamaismail.com",           status:500, uri:"/api/internal",                src_ip:"10.9.9.0",   bytes:393,   user:"-",         method:"POST", sourcetype:"access_combined", action:"error",    session:"sess_oo" },
    { _time:"2025-09-09 09:09:09", host:"wadeehaddad.com",           status:200, uri:"/projects/9x3",                src_ip:"10.9.3.9",   bytes:99999, user:"wadee",     method:"GET",  sourcetype:"access_combined", action:"success",  session:"sess_ww" },
    { _time:"2025-09-09 09:39:09", host:"artifacts.qayssarayra.com", status:200, uri:"/artifacts/hidden/qays9",      src_ip:"10.9.0.9",   bytes:93933, user:"qays9",     method:"GET",  sourcetype:"access_combined", action:"success",  session:"sess_9a" },
    { _time:"2025-09-09 09:00:33", host:"ctfs.fun",                  status:200, uri:"/ctf/forensics/memory_9",      src_ip:"10.0.9.9",   bytes:9393,  user:"hidden",    method:"POST", sourcetype:"access_combined", action:"success",  session:"sess_3b" },
    { _time:"2025-09-09 09:39:03", host:"qays.fun",                  status:403, uri:"/admin",                       src_ip:"192.168.0.9",bytes:993,   user:"-",         method:"GET",  sourcetype:"access_combined", action:"forbidden",session:"sess_xx" }
  ],

  categories: [
    { id:"transform", label:"Transforming",     color:"var(--accent-cyan)",   icon:"⚡" },
    { id:"filter",    label:"Filtering",        color:"var(--accent-green)",  icon:"🔍" },
    { id:"correlate", label:"Correlating",      color:"var(--accent-orange)", icon:"🔗" },
    { id:"extract",   label:"Field Extraction", color:"var(--accent-purple)", icon:"🧩" },
    { id:"enrich",    label:"Enrichment",       color:"var(--accent-yellow)", icon:"✨" }
  ],

  commands: {

    stats: {
      name:"stats", icon:"∑", category:"Transforming", tag:"transform", tagLabel:"TRANSFORM", tagClass:"tag-transform",
      subtitle:"Provides statistics grouped optionally by fields",
      badges:["Transforming","Aggregation","Reporting"],
      description:`The <strong>stats</strong> command computes statistics across all events or grouped by fields via the <span class="spl-keyword">by</span> clause. It is the <em>most efficient</em> aggregation command — faster and more scalable than <code>transaction</code> in large environments. Stats functions: <code>count</code>, <code>sum</code>, <code>avg</code>, <code>max</code>, <code>min</code>, <code>dc</code> (distinct count), <code>values</code>, <code>list</code>, <code>stdev</code>, <code>var</code>, <code>range</code>, <code>mode</code>, <code>median</code>, <code>perc&lt;X&gt;</code>, <code>earliest</code>, <code>latest</code>. Field names can be wildcarded: <code>avg(*delay)</code>.`,
      syntax:[
        "index=web_logs | stats count by status",
        "index=web_logs | stats count, sum(bytes) as total_bytes by host",
        "index=web_logs | stats dc(user) as unique_users avg(bytes) by host",
        "index=web_logs | stats values(uri) as pages by user"
      ],
      options:[
        { opt:"count(X)", desc:"Count events; count(*) = count all" },
        { opt:"sum(X)", desc:"Sum of numeric field X" },
        { opt:"avg(X)", desc:"Average of field X" },
        { opt:"dc(X)", desc:"Distinct count — unique values of X" },
        { opt:"values(X)", desc:"All distinct values as multi-value" },
        { opt:"list(X)", desc:"All values including duplicates" },
        { opt:"max(X)/min(X)", desc:"Maximum / minimum value" },
        { opt:"range(X)", desc:"Difference between max and min" },
        { opt:"stdev(X)", desc:"Sample standard deviation" },
        { opt:"mode(X)", desc:"Most frequent value" },
        { opt:"median(X)", desc:"Middle-most value" },
        { opt:"perc5(X)", desc:"5th percentile; any perc<N> works" },
        { opt:"earliest(X)/latest(X)", desc:"Chronologically first/last value" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> Use <code>stats</code> for aggregated numbers. Use <code>transaction</code> only when you need correlated events as a single event." },
        { text:"<strong>EXAM TIP:</strong> <code>stats</code> is faster and more efficient than <code>transaction</code> in large environments." },
        { text:"<strong>EXAM TIP:</strong> <code>dc(X)</code> = distinct count. Not the same as <code>count(X)</code>." }
      ],
      presets:[
        { label:"count by status",     query:"index=web_logs\n| stats count by status" },
        { label:"sum(bytes) by host",  query:"index=web_logs\n| stats sum(bytes) as total_bytes by host" },
        { label:"dc(user) by host",    query:"index=web_logs\n| stats dc(user) as unique_users by host" },
        { label:"values(uri) by user", query:"index=web_logs\n| stats values(uri) as pages by user" }
      ],
      defaultQuery:"index=web_logs\n| stats count by status"
    },

    chart: {
      name:"chart", icon:"◫", category:"Transforming", tag:"transform", tagLabel:"TRANSFORM", tagClass:"tag-transform",
      subtitle:"Returns results in a tabular output for charting",
      badges:["Transforming","Visualization","Aggregation"],
      description:`The <strong>chart</strong> command produces a table optimized for visualization. The <span class="spl-keyword">over</span> clause defines the X-axis (row values); the <span class="spl-keyword">by</span> clause creates series columns. Key equivalence: <code>chart count by status, host</code> ≡ <code>chart count over status by host</code>. Stack mode is set via the <em>Format menu</em> — not via SPL.`,
      syntax:[
        "index=web_logs | chart count by status",
        "index=web_logs | chart count by status, host",
        "index=web_logs | chart avg(bytes) over host by status",
        "index=web_logs | chart count by status limit=5 useother=false"
      ],
      options:[
        { opt:"over <field>", desc:"X-axis values (rows in result table)" },
        { opt:"by <field>", desc:"Series columns; combined with over or alone" },
        { opt:"limit=<N>", desc:"Max number of series (default 10)" },
        { opt:"useother=false", desc:"Remove the OTHER catch-all bucket" },
        { opt:"usenull=false", desc:"Remove NULL value bucket" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> Stack mode is set via <em>Format menu</em> — NOT an SPL argument. Trellis ≠ stack mode." },
        { text:"<strong>EXAM TIP:</strong> <code>chart count by vendor_action, user</code> produces identical results to <code>chart count over vendor_action by user</code>." }
      ],
      presets:[
        { label:"count by status",      query:"index=web_logs\n| chart count by status" },
        { label:"count by host,status", query:"index=web_logs\n| chart count by host, status" },
        { label:"avg(bytes) by host",   query:"index=web_logs\n| chart avg(bytes) over host by status" }
      ],
      defaultQuery:"index=web_logs\n| chart count by status"
    },

    timechart: {
      name:"timechart", icon:"↗", category:"Transforming", tag:"transform", tagLabel:"TRANSFORM", tagClass:"tag-transform",
      subtitle:"Creates time-series charts — _time is always the X-axis",
      badges:["Transforming","Time-Series","Visualization"],
      description:`The <strong>timechart</strong> command plots data over time using <span class="spl-keyword">_time</span> as the automatic X-axis. Because time is already the X-axis, only <strong>one field</strong> may follow a <span class="spl-keyword">by</span> clause. The <span class="spl-keyword">span</span> argument controls bucket size — not <code>duration</code> or <code>interval</code> (exam distractors).`,
      syntax:[
        "index=web_logs | timechart count",
        "index=web_logs | timechart count by host",
        "index=web_logs | timechart span=1h avg(bytes) by host",
        "index=web_logs | timechart span=30m count by host limit=5"
      ],
      options:[
        { opt:"span=<time>", desc:"Bucket size: 1h, 5m, 1d, 30m, etc." },
        { opt:"by <field>", desc:"Split by ONE field only (_time = X-axis)" },
        { opt:"limit=<N>", desc:"Max series shown" },
        { opt:"useother=false", desc:"Hide OTHER bucket beyond limit" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> Only ONE field after <code>by</code> in timechart." },
        { text:"<strong>EXAM TIP:</strong> <code>span</code> is correct. <code>duration</code> and <code>interval</code> are distractors." },
        { text:"<strong>EXAM TIP:</strong> X-axis field = <code>_time</code>. Not <code>date</code>, not <code>time</code>." }
      ],
      presets:[
        { label:"timechart count",    query:"index=web_logs\n| timechart count" },
        { label:"count by host",      query:"index=web_logs\n| timechart count by host" },
        { label:"span=1h avg(bytes)", query:"index=web_logs\n| timechart span=1h avg(bytes) by host" }
      ],
      defaultQuery:"index=web_logs\n| timechart count by host"
    },

    eval: {
      name:"eval", icon:"⚙", category:"Transforming", tag:"transform", tagLabel:"TRANSFORM", tagClass:"tag-transform",
      subtitle:"Calculates expressions and creates or replaces fields",
      badges:["Transforming","Field Creation","Expressions"],
      description:`The <strong>eval</strong> command creates new fields or modifies existing ones. Results are stored <em>in a field</em> (not in an index or KV Store). Supports arithmetic, string concatenation (<code>.</code>), boolean logic, and functions: <code>if()</code>, <code>case()</code>, <code>coalesce()</code>, <code>round()</code>, <code>len()</code>, <code>lower()</code>, <code>upper()</code>, <code>substr()</code>, <code>replace()</code>, <code>tonumber()</code>, <code>tostring()</code>, <code>strftime()</code>, <code>now()</code>, <code>cidrmatch()</code>, <code>mvcount()</code>, <code>isnull()</code>, <code>match()</code>, <code>md5()</code>, and more. <code>tostring</code> accepts: <em>hex</em>, <em>commas</em>, <em>duration</em>. <strong>"decimal" is NOT valid.</strong>`,
      syntax:[
        'index=web_logs | eval kb = round(bytes/1024, 2)',
        'index=web_logs | eval label = if(status==200, "OK", "Error")',
        'index=web_logs | eval label = case(status==200,"OK",status==404,"Not Found",status==500,"Server Error",1=1,"Other")',
        'index=web_logs | eval display = tostring(bytes, "commas")'
      ],
      options:[
        { opt:"if(X,Y,Z)", desc:"If X is true → Y, else → Z" },
        { opt:"case(X1,Y1,...)", desc:"Multi-branch conditional; returns first true Y" },
        { opt:"coalesce(X,...)", desc:"Returns first non-null value" },
        { opt:"round(X,Y)", desc:"Round X to Y decimal places" },
        { opt:'tostring(X,"hex|commas|duration")', desc:"Convert number to hex, comma-formatted, or HH:MM:SS" },
        { opt:"tonumber(X,Y)", desc:"Parse string X in base Y (default 10)" },
        { opt:"strftime(X,Y)", desc:"Format epoch X using format Y" },
        { opt:"len(X)", desc:"Character length of string X" },
        { opt:"lower(X)/upper(X)", desc:"Case conversion" },
        { opt:"substr(X,Y,Z)", desc:"Substring from position Y for Z chars (1-based)" },
        { opt:"match(X,Y)", desc:"Returns TRUE if X matches regex Y" },
        { opt:"cidrmatch(X,Y)", desc:"TRUE if IP Y belongs to subnet X" },
        { opt:"isnull(X)/isnotnull(X)", desc:"Null value checks" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> eval results are stored <em>in a field</em> — not in index, KV Store, or database." },
        { text:'<strong>EXAM TIP:</strong> <code>tostring(X, "hex")</code>, <code>"commas"</code>, <code>"duration"</code> are valid. <code>"decimal"</code> is NOT.' },
        { text:"<strong>EXAM TIP:</strong> eval creates/replaces fields. To remove fields use <code>fields - fieldname</code>." }
      ],
      presets:[
        { label:"eval kb",          query:'index=web_logs\n| eval kb = round(bytes/1024, 2)\n| table host, uri, kb' },
        { label:"eval if()",        query:'index=web_logs\n| eval label = if(status==200, "OK", "Error")\n| table host, status, label' },
        { label:"eval case()",      query:'index=web_logs\n| eval label = case(status==200,"OK",status==404,"Not Found",status==500,"Server Error",1=1,"Other")\n| table host, status, label' },
        { label:"eval tostring()",  query:'index=web_logs\n| eval display = tostring(bytes, "commas")\n| table host, bytes, display' }
      ],
      defaultQuery:'index=web_logs\n| eval kb = round(bytes/1024, 2)\n| table host, uri, kb'
    },

    where: {
      name:"where", icon:"▷", category:"Filtering", tag:"filter", tagLabel:"FILTER", tagClass:"tag-filter",
      subtitle:"Filters results using eval expressions — can compare two fields",
      badges:["Filtering","Comparison","Boolean"],
      description:`The <strong>where</strong> command filters results using eval-style expressions. Its key advantage: it can <strong>compare two different fields</strong> — something the <code>search</code> command cannot do. Supports boolean operators: <code>AND</code>, <code>OR</code>, <code>NOT</code>. Supports comparisons: <code>&lt;</code>, <code>&gt;</code>, <code>&lt;=</code>, <code>&gt;=</code>, <code>!=</code>, <code>==</code>, <code>LIKE</code>. Also supports all eval functions: <code>isnull()</code>, <code>isnotnull()</code>, <code>match()</code>, <code>cidrmatch()</code>, <code>like()</code>.`,
      syntax:[
        "index=web_logs | where status > 400",
        "index=web_logs | where bytes > 50000 AND status=200",
        'index=web_logs | where isnotnull(user) AND user != "-"',
        'index=web_logs | where like(uri, "%/ctf/%")'
      ],
      options:[
        { opt:"X > Y", desc:"Comparison: > < >= <= != ==" },
        { opt:"AND / OR / NOT", desc:"Boolean logic" },
        { opt:"isnull(X)", desc:"Filter for null/empty fields" },
        { opt:"isnotnull(X)", desc:"Filter for non-null fields" },
        { opt:"like(X,Y)", desc:"SQLite LIKE pattern matching with %" },
        { opt:"match(X,Y)", desc:"Regex-based filtering" },
        { opt:"cidrmatch(X,Y)", desc:"Filter IPs by subnet" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> <code>where</code> can compare TWO different fields — <code>search</code> cannot. This is its primary advantage." },
        { text:"<strong>EXAM TIP:</strong> <code>where</code> uses eval-style expressions; field values ARE case-sensitive in comparisons." }
      ],
      presets:[
        { label:"status > 400",          query:"index=web_logs\n| where status > 400\n| table host, uri, status, user" },
        { label:"bytes > 50000",         query:"index=web_logs\n| where bytes > 50000 AND status=200\n| table host, bytes, status" },
        { label:'like(uri, "%ctf%")',    query:'index=web_logs\n| where like(uri, "%/ctf/%")\n| table host, uri, user' }
      ],
      defaultQuery:"index=web_logs\n| where status > 400\n| table host, uri, status, user"
    },

    transaction: {
      name:"transaction", icon:"◎", category:"Correlating", tag:"correlate", tagLabel:"CORRELATE", tagClass:"tag-correlate",
      subtitle:"Groups related events into a single correlated event",
      badges:["Correlating","Session Analysis","Event Grouping"],
      description:`The <strong>transaction</strong> command groups related events into one based on shared fields and time constraints. Automatically adds <span class="spl-keyword">duration</span> (seconds between first/last event) and <span class="spl-keyword">eventcount</span> (number of events). Events in a transaction do NOT need the same sourcetype, timestamp, or field set — only <em>related by one or more fields</em>. Limited to 1000 events per group. Slower than <code>stats</code> — prefer <code>stats</code> in large environments.`,
      syntax:[
        "index=web_logs | transaction session",
        "index=web_logs | transaction session maxspan=30s",
        'index=web_logs | transaction session startswith="GET" endswith="POST"',
        "index=web_logs | transaction host session maxspan=30s maxpause=5s"
      ],
      options:[
        { opt:"<field>", desc:"One or more fields to group by (e.g. session, user)" },
        { opt:"maxspan=<time>", desc:"Max total time between earliest and latest event" },
        { opt:"maxpause=<time>", desc:"Max gap between consecutive events" },
        { opt:"startswith=<str>", desc:"String/eval pattern in first event" },
        { opt:"endswith=<str>", desc:"String/eval pattern in last event" },
        { opt:"maxevents=<N>", desc:"Max events per transaction (default 1000)" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> Transaction creates <code>duration</code> and <code>eventcount</code> automatically. NOT <code>maxspan</code> as a field." },
        { text:"<strong>EXAM TIP:</strong> Events must be related by fields — not same sourcetype/timestamp." },
        { text:"<strong>EXAM TIP:</strong> 1000-event limit per transaction group. Use <code>stats</code> if groups exceed this." },
        { text:"<strong>EXAM TIP:</strong> Use <code>startswith</code>/<code>endswith</code> when grouping is based on start/end values." }
      ],
      presets:[
        { label:"transaction session",   query:"index=web_logs\n| transaction session" },
        { label:"maxspan=30s",           query:"index=web_logs\n| transaction session maxspan=30s" },
        { label:"search after tx",       query:"index=web_logs\n| transaction session\n| search action=forbidden" }
      ],
      defaultQuery:"index=web_logs\n| transaction session"
    },

    top: {
      name:"top", icon:"▲", category:"Transforming", tag:"transform", tagLabel:"TRANSFORM", tagClass:"tag-transform",
      subtitle:"Displays the most common values of a field",
      badges:["Transforming","Frequency","Ranking"],
      description:`The <strong>top</strong> command returns the most frequent values of a field with automatic <code>count</code> and <code>percent</code> columns. Its counterpart <strong>rare</strong> returns the least common values. Both output count and percent by default.`,
      syntax:[
        "index=web_logs | top host",
        "index=web_logs | top limit=5 host",
        "index=web_logs | top limit=20 uri",
        "index=web_logs | rare status"
      ],
      options:[
        { opt:"limit=<N>", desc:"How many values to return (default 10)" },
        { opt:"showcount=false", desc:"Hide the count column" },
        { opt:"showperc=false", desc:"Hide the percent column" },
        { opt:"useother=false", desc:"Remove the OTHER row" },
        { opt:"countfield=<n>", desc:"Rename the count output field" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> <code>top</code> = most common. <code>rare</code> = least common. Both include count and percent." }
      ],
      presets:[
        { label:"top host",        query:"index=web_logs\n| top host" },
        { label:"top limit=5 uri", query:"index=web_logs\n| top limit=5 uri" },
        { label:"rare status",     query:"index=web_logs\n| rare status" }
      ],
      defaultQuery:"index=web_logs\n| top host"
    },

    dedup: {
      name:"dedup", icon:"⊘", category:"Filtering", tag:"filter", tagLabel:"FILTER", tagClass:"tag-filter",
      subtitle:"Removes subsequent duplicate events matching specified fields",
      badges:["Filtering","Deduplication","Unique Values"],
      description:`The <strong>dedup</strong> command removes duplicate events based on specified field values, keeping only the first occurrence by default. Multiple fields: dedup removes events where ALL listed fields match a previous result. Use <code>sortby</code> to control which duplicate is kept.`,
      syntax:[
        "index=web_logs | dedup host",
        "index=web_logs | dedup user status",
        "index=web_logs | dedup host sortby -bytes",
        "index=web_logs | dedup 3 host"
      ],
      options:[
        { opt:"<field> [<field>...]", desc:"Fields to deduplicate by" },
        { opt:"<N> <field>", desc:"Keep up to N duplicates per value" },
        { opt:"sortby <field>", desc:"Sort before dedup to control which is kept" },
        { opt:"keepevents=true", desc:"Keep duplicates but mark them null" },
        { opt:"keepempty=true", desc:"Keep events with null field values" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> <code>dedup</code> keeps the FIRST occurrence by default. Use <code>sortby</code> to control which duplicate is retained." }
      ],
      presets:[
        { label:"dedup host",          query:"index=web_logs\n| dedup host\n| table host, uri, status" },
        { label:"dedup user status",   query:"index=web_logs\n| dedup user status\n| table user, status, host" },
        { label:"dedup sortby -bytes", query:"index=web_logs\n| dedup host sortby -bytes\n| table host, bytes, uri" }
      ],
      defaultQuery:"index=web_logs\n| dedup host\n| table host, uri, status"
    },

    sort: {
      name:"sort", icon:"⇅", category:"Filtering", tag:"filter", tagLabel:"FILTER", tagClass:"tag-filter",
      subtitle:"Sorts search results by specified fields",
      badges:["Filtering","Ordering","Results"],
      description:`The <strong>sort</strong> command sorts results by one or more fields. Prefix with <code>-</code> for descending (default is ascending). Multi-field sort applies in order. Use <code>limit</code> to return only top N sorted results.`,
      syntax:[
        "index=web_logs | sort bytes",
        "index=web_logs | sort -bytes",
        "index=web_logs | sort host, -bytes",
        "index=web_logs | sort limit=10 -bytes"
      ],
      options:[
        { opt:"-<field>", desc:"Descending order (largest first)" },
        { opt:"+<field>", desc:"Ascending order (explicit, this is default)" },
        { opt:"limit=<N>", desc:"Return only top N results after sort" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> <code>sort ip, -url</code> = sort by IP ascending THEN by URL descending. Field order matters." }
      ],
      presets:[
        { label:"sort -bytes",       query:"index=web_logs\n| sort -bytes\n| table host, uri, bytes, status" },
        { label:"sort host, -bytes", query:"index=web_logs\n| sort host, -bytes\n| table host, bytes, uri" },
        { label:"sort limit=5",      query:"index=web_logs\n| sort limit=5 -bytes\n| table host, bytes, uri" }
      ],
      defaultQuery:"index=web_logs\n| sort -bytes\n| table host, uri, bytes, status"
    },

    rex: {
      name:"rex", icon:"◈", category:"Field Extraction", tag:"extract", tagLabel:"EXTRACT", tagClass:"tag-extract",
      subtitle:"Extracts fields using Perl regex named capture groups",
      badges:["Field Extraction","Regex","Named Groups"],
      description:`The <strong>rex</strong> command extracts fields using Perl-compatible regular expressions with <em>named capture groups</em>: <code>(?P&lt;fieldname&gt;...)</code>. By default it operates on <code>_raw</code> — specify <code>field=</code> to target another field. After a manual regex edit, the FX UI can no longer edit that extraction.`,
      syntax:[
        'index=web_logs | rex field=uri "(?P<path>[^?]+)"',
        'index=web_logs | rex field=src_ip "(?P<subnet>\\d+\\.\\d+\\.\\d+)"',
        'index=web_logs | rex field=uri "/(?P<section>[^/]+)"',
        'index=web_logs | rex "From: (?P<from>.*) To: (?P<to>.*)"'
      ],
      options:[
        { opt:"field=<field>", desc:"Source field (default: _raw)" },
        { opt:"(?P<n>...)", desc:"Named capture group — creates field n" },
        { opt:"mode=sed", desc:"sed-mode: find/replace on fields" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> Named groups use <code>(?P&lt;name&gt;...)</code> syntax." },
        { text:"<strong>EXAM TIP:</strong> After manually editing a regex, the FX UI can NO LONGER edit that extraction." }
      ],
      presets:[
        { label:"extract path",   query:'index=web_logs\n| rex field=uri "(?P<path>[^?]+)"\n| table host, uri, path' },
        { label:"extract subnet", query:'index=web_logs\n| rex field=src_ip "(?P<subnet>\\d+\\.\\d+\\.\\d+)"\n| table src_ip, subnet, host' }
      ],
      defaultQuery:'index=web_logs\n| rex field=uri "(?P<path>[^?]+)"\n| table host, uri, path'
    },

    table: {
      name:"table", icon:"⊞", category:"Filtering", tag:"filter", tagLabel:"FILTER", tagClass:"tag-filter",
      subtitle:"Retains specified fields in tabular format",
      badges:["Filtering","Display","Field Selection"],
      description:`The <strong>table</strong> command specifies which fields to include in results and displays them as a table. Fields appear in the order listed. Unlike <code>fields +</code>, table always forces tabular output with only the named columns.`,
      syntax:[
        "index=web_logs | table host, uri, status",
        "index=web_logs | table _time, user, method, uri, status, bytes",
        "index=web_logs | stats count by host | table host, count"
      ],
      options:[
        { opt:"<field> [<field>...]", desc:"Fields to include, in display order" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> <code>table</code> = keep specific fields AND force tabular view. <code>fields +</code> = keep fields but doesn't force table format." }
      ],
      presets:[
        { label:"basic table",  query:"index=web_logs\n| table host, uri, status, bytes" },
        { label:"full fields",  query:"index=web_logs\n| table _time, host, user, method, uri, status, bytes" },
        { label:"after stats",  query:"index=web_logs\n| stats count by host\n| table host, count" }
      ],
      defaultQuery:"index=web_logs\n| table host, uri, status, bytes"
    },

    fields: {
      name:"fields", icon:"⊟", category:"Filtering", tag:"filter", tagLabel:"FILTER", tagClass:"tag-filter",
      subtitle:"Removes or keeps specific fields from search results",
      badges:["Filtering","Field Management","Performance"],
      description:`The <strong>fields</strong> command includes (<code>+</code>) or excludes (<code>-</code>) fields. Using <code>fields +</code> early in the pipeline improves performance by reducing data transferred between pipeline stages. Using <code>fields -</code> removes unwanted columns.`,
      syntax:[
        "index=web_logs | fields host, uri, status",
        "index=web_logs | fields + host, uri, status",
        "index=web_logs | fields - src_ip, session, action"
      ],
      options:[
        { opt:"+ <fields>", desc:"Keep only listed fields (default if no +/-)" },
        { opt:"- <fields>", desc:"Remove listed fields from results" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> Using <code>fields +</code> early in a search improves performance by limiting data movement." }
      ],
      presets:[
        { label:"fields keep",   query:"index=web_logs\n| fields + host, uri, status, bytes" },
        { label:"fields remove", query:"index=web_logs\n| fields - session, action, sourcetype" }
      ],
      defaultQuery:"index=web_logs\n| fields + host, uri, status, bytes"
    },

    fillnull: {
      name:"fillnull", icon:"□", category:"Enrichment", tag:"enrich", tagLabel:"ENRICH", tagClass:"tag-enrich",
      subtitle:"Replaces null values with a specified value (default: 0)",
      badges:["Enrichment","Null Handling","Data Cleanup"],
      description:`The <strong>fillnull</strong> command replaces null field values. The default replacement value is <strong>0</strong> (zero) — not NULL, not empty string, not N/A. Specify a custom <code>value=</code> string. Optionally target specific fields.`,
      syntax:[
        "index=web_logs | fillnull",
        'index=web_logs | fillnull value="UNKNOWN"',
        "index=web_logs | fillnull value=0 bytes",
        'index=web_logs | fillnull value="N/A" user, method'
      ],
      options:[
        { opt:"value=<str>", desc:"Replacement value (default: 0)" },
        { opt:"<field> [...]", desc:"Specific fields to fill (default: all null fields)" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> Default <code>fillnull</code> replacement value is <strong>0</strong> (zero). Not NULL, not empty string, not N/A." }
      ],
      presets:[
        { label:"fillnull default",       query:"index=web_logs\n| fillnull\n| table host, user, bytes" },
        { label:'fillnull value=UNKNOWN', query:'index=web_logs\n| fillnull value="UNKNOWN"\n| table host, user, action' },
        { label:"fillnull specific",      query:'index=web_logs\n| fillnull value="-" user\n| table host, user, status' }
      ],
      defaultQuery:'index=web_logs\n| fillnull value="UNKNOWN"\n| table host, user, action'
    },

    rename: {
      name:"rename", icon:"✎", category:"Filtering", tag:"filter", tagLabel:"FILTER", tagClass:"tag-filter",
      subtitle:"Renames fields; use wildcards for multiple fields",
      badges:["Filtering","Field Management","Normalization"],
      description:`The <strong>rename</strong> command renames one or more fields. After renaming, the <em>original field name no longer works</em> — use only the new name in subsequent pipeline stages. Wildcards (<code>*</code>) rename multiple fields matching a pattern.`,
      syntax:[
        "index=web_logs | rename host as hostname",
        "index=web_logs | rename src_ip as client_ip, bytes as size",
        "index=web_logs | rename host as hostname | table hostname, uri",
        "index=web_logs | stats count by host | rename host as server, count as requests"
      ],
      options:[
        { opt:"<old> as <new>", desc:"Rename old field to new name" },
        { opt:"wildcards", desc:"Use * to match/rename multiple fields" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> After <code>rename</code>, use the NEW name in all subsequent commands. Old name is gone." }
      ],
      presets:[
        { label:"rename host",        query:"index=web_logs\n| rename host as hostname\n| table hostname, uri, status" },
        { label:"rename multiple",    query:"index=web_logs\n| rename src_ip as client_ip, bytes as size\n| table hostname, client_ip, size" },
        { label:"rename after stats", query:"index=web_logs\n| stats count by host\n| rename host as server, count as requests" }
      ],
      defaultQuery:"index=web_logs\n| rename host as hostname, bytes as size_bytes\n| table hostname, uri, size_bytes, status"
    },

    lookup: {
      name:"lookup", icon:"◉", category:"Enrichment", tag:"enrich", tagLabel:"ENRICH", tagClass:"tag-enrich",
      subtitle:"Adds field values from an external lookup table",
      badges:["Enrichment","Lookup Tables","Knowledge Objects"],
      description:`The <strong>lookup</strong> command enriches events with data from CSV files, KV Store, or geo lookups by matching on a key field. The lookup file must be defined in <code>transforms.conf</code>. <code>inputlookup</code> reads a lookup file directly; <code>outputlookup</code> writes results back. Knowledge object order: <em>Field Extractions → Field Aliases → Lookups</em>. CIM uses field aliases, event types, tags, lookups, and field extractions for normalization.`,
      syntax:[
        "index=web_logs | lookup usertogroup user OUTPUT group",
        "index=web_logs | lookup statusmap.csv status OUTPUT label",
        "| inputlookup usertogroup",
        "index=web_logs | outputlookup my_results.csv"
      ],
      options:[
        { opt:"<lookup_name> <match_field>", desc:"Lookup name and field to match on" },
        { opt:"OUTPUT <field>", desc:"Fields to output from the lookup table" },
        { opt:"OUTPUTNEW <field>", desc:"Only populate if field is currently null" },
        { opt:"inputlookup", desc:"Load a lookup file as the search dataset" },
        { opt:"outputlookup", desc:"Write search results to a lookup file" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> Knowledge object order: <strong>Field Extractions → Field Aliases → Lookups</strong>. Lookups are applied LAST." },
        { text:"<strong>EXAM TIP:</strong> CIM uses field aliases, event types, tags, <strong>lookups</strong>, AND field extractions for data normalization." }
      ],
      presets:[
        { label:"lookup simulation",  query:"index=web_logs\n| stats count by user\n| rename user as username" },
        { label:"inputlookup sim",    query:"| inputlookup users.csv" },
        { label:"outputlookup sim",   query:"index=web_logs\n| stats count by host\n| outputlookup host_counts.csv" }
      ],
      defaultQuery:"index=web_logs\n| stats count by user\n| rename user as username, count as requests"
    },

    rare: {
      name:"rare", icon:"◇", category:"Transforming", tag:"transform", tagLabel:"TRANSFORM", tagClass:"tag-transform",
      subtitle:"Displays the least common values of a field",
      badges:["Transforming","Frequency","Ranking"],
      description:`The <strong>rare</strong> command returns the least frequent values of a field — the inverse of <code>top</code>. Both commands automatically include <code>count</code> and <code>percent</code> columns. You do not add them manually. Supports a <code>by</code> clause for grouped rare values.`,
      syntax:[
        "index=web_logs | rare status",
        "index=web_logs | rare limit=5 action",
        "index=web_logs | rare method by host",
        "index=web_logs | rare user limit=3"
      ],
      options:[
        { opt:"limit=<N>", desc:"How many values to return (default 10)" },
        { opt:"by <field>", desc:"Group rare values by a field" },
        { opt:"showcount=false", desc:"Hide the count column" },
        { opt:"showperc=false", desc:"Hide the percent column" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> <code>rare</code> = least common values. <code>top</code> = most common. Both auto-include count and percent." },
        { text:"<strong>EXAM TIP:</strong> You do NOT need to manually add count/percent — they are automatic." }
      ],
      presets:[
        { label:"rare status",      query:"index=web_logs\n| rare status" },
        { label:"rare limit=3 uri", query:"index=web_logs\n| rare limit=3 uri" },
        { label:"rare user",        query:"index=web_logs\n| rare user" }
      ],
      defaultQuery:"index=web_logs\n| rare status"
    },

    search: {
      name:"search", icon:"⌕", category:"Filtering", tag:"filter", tagLabel:"FILTER", tagClass:"tag-filter",
      subtitle:"Filters events by keyword, field value, or boolean expression",
      badges:["Filtering","Keywords","Inline"],
      description:`The <strong>search</strong> command filters results using keywords, field=value pairs, wildcards, and boolean operators. It can appear anywhere in the pipeline — not just at the start. A space between terms is an implied <code>AND</code>. Use <code>where</code> after <code>stats</code> to filter computed fields with expressions — <code>search</code> treats values as strings, not expressions. Wildcards: trailing wildcards work (<code>err*</code>); leading wildcards do not (<code>*error</code> is slow/unreliable).`,
      syntax:[
        "index=web_logs | search status=404",
        'index=web_logs | search user=qays9 method=GET',
        "index=web_logs | stats count by host | search count > 3",
        'index=web_logs | search uri=*/ctf/*'
      ],
      options:[
        { opt:"<keyword>", desc:"Keyword search across all fields" },
        { opt:"field=value", desc:"Exact field value match" },
        { opt:"field=val*", desc:"Wildcard — trailing wildcards only" },
        { opt:"NOT field=value", desc:"Exclude matching events" },
        { opt:"AND / OR", desc:"Boolean operators (AND is implied by space)" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> A space in a search string is an implied AND. <code>error login</code> = <code>error AND login</code>." },
        { text:"<strong>EXAM TIP:</strong> Use <code>where</code> (not <code>search</code>) after <code>stats</code> to filter with expressions like <code>count > 10</code>." },
        { text:"<strong>EXAM TIP:</strong> <code>search</code> can appear anywhere in the pipeline — it is not restricted to the beginning." }
      ],
      presets:[
        { label:"search status=404",  query:"index=web_logs\n| search status=404\n| table host, uri, status, user" },
        { label:"search user=qays9",  query:"index=web_logs\n| search user=qays9\n| table host, uri, method, bytes" },
        { label:"after stats",        query:"index=web_logs\n| stats count by host\n| search count > 3" }
      ],
      defaultQuery:"index=web_logs\n| search status=404\n| table host, uri, status, user"
    },

    head: {
      name:"head", icon:"⊤", category:"Filtering", tag:"filter", tagLabel:"FILTER", tagClass:"tag-filter",
      subtitle:"Returns the first N results",
      badges:["Filtering","Results","Limiting"],
      description:`The <strong>head</strong> command returns the first N results from the result set. Default is 10. Its counterpart <strong>tail</strong> returns the last N results. Combine with <code>sort</code> to get top/bottom values by a field.`,
      syntax:[
        "index=web_logs | head 10",
        "index=web_logs | head 5",
        "index=web_logs | sort -bytes | head 10",
        "index=web_logs | tail 5"
      ],
      options:[
        { opt:"<N>", desc:"Number of results to return (default 10)" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> Default for both <code>head</code> and <code>tail</code> is 10 if no number is specified." },
        { text:"<strong>EXAM TIP:</strong> <code>sort -bytes | head 10</code> = top 10 by bytes descending." }
      ],
      presets:[
        { label:"head 5",              query:"index=web_logs\n| head 5" },
        { label:"sort -bytes | head",  query:"index=web_logs\n| sort -bytes\n| head 5\n| table host, uri, bytes" },
        { label:"tail 5",              query:"index=web_logs\n| tail 5" }
      ],
      defaultQuery:"index=web_logs\n| sort -bytes\n| head 5\n| table host, uri, bytes, status"
    },

    reverse: {
      name:"reverse", icon:"⇄", category:"Filtering", tag:"filter", tagLabel:"FILTER", tagClass:"tag-filter",
      subtitle:"Reverses the order of the current result set",
      badges:["Filtering","Ordering","Results"],
      description:`The <strong>reverse</strong> command flips the current result set order. Because Splunk returns events in reverse chronological order by default, piping <code>| reverse</code> produces chronological (oldest first) order without specifying a sort field.`,
      syntax:[
        "index=web_logs | reverse",
        "index=web_logs | sort -bytes | reverse",
        "index=web_logs | head 10 | reverse"
      ],
      options:[],
      tips:[
        { text:"<strong>EXAM TIP:</strong> Default order is reverse chronological (newest first). <code>| reverse</code> gives you chronological order." }
      ],
      presets:[
        { label:"reverse",            query:"index=web_logs\n| reverse\n| table _time, host, uri, status" },
        { label:"head then reverse",  query:"index=web_logs\n| head 10\n| reverse\n| table _time, host, uri" }
      ],
      defaultQuery:"index=web_logs\n| reverse\n| table _time, host, uri, status"
    },

    append: {
      name:"append", icon:"⊕", category:"Correlating", tag:"correlate", tagLabel:"CORRELATE", tagClass:"tag-correlate",
      subtitle:"Appends subsearch results as additional rows",
      badges:["Correlating","Subsearch","Row Append"],
      description:`The <strong>append</strong> command adds the results of a subsearch as new rows to the current result set. It does NOT join on a field — rows are simply stacked. The subsearch runs independently and its output is appended at the bottom. Unlike <code>appendcols</code> (which adds columns by position), <code>append</code> adds rows. Use <code>join</code> to merge on a shared key field instead.`,
      syntax:[
        "index=web_logs status=200 | stats count as success by host | append [search index=web_logs status>=400 | stats count as errors by host]",
        "index=web_logs | append [search index=web_logs user=attacker]",
        "index=web_logs | stats count by host | append [| inputlookup hosts_meta]"
      ],
      options:[
        { opt:"[ subsearch ]", desc:"Subsearch whose results are appended as rows" },
        { opt:"maxtime=<N>", desc:"Max seconds the subsearch may run (default 60)" },
        { opt:"maxout=<N>", desc:"Max events from subsearch (default 50000)" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> <code>append</code> adds rows. <code>appendcols</code> adds columns. <code>join</code> merges on a key field." },
        { text:"<strong>EXAM TIP:</strong> Subsearch limit: 10,000 results, 60 second timeout by default." }
      ],
      presets:[
        { label:"append rows",    query:"index=web_logs\n| stats count as requests by host\n| append\n[search index=web_logs status>=400\n| stats count as errors by host]" },
        { label:"append lookup",  query:"index=web_logs\n| stats count by host\n| append [| inputlookup hosts_meta]" }
      ],
      defaultQuery:"index=web_logs\n| stats count as requests by host\n| append [| inputlookup hosts_meta]"
    },

    join: {
      name:"join", icon:"⋈", category:"Correlating", tag:"correlate", tagLabel:"CORRELATE", tagClass:"tag-correlate",
      subtitle:"Joins subsearch results on a shared field — like SQL JOIN",
      badges:["Correlating","Subsearch","SQL-style Join"],
      description:`The <strong>join</strong> command merges results of a subsearch with the current results based on a shared key field. Similar to SQL JOIN. <code>type=inner</code> keeps only matching rows (default). <code>type=left</code> keeps all main results, filling nulls where no match. Join can be slow on large datasets — prefer <code>lookup</code> or <code>stats</code> when possible.`,
      syntax:[
        "index=web_logs | stats count by user | join user [| inputlookup usertogroup]",
        "index=web_logs | join type=left user [| inputlookup usertogroup]",
        "index=web_logs | join type=inner host [| inputlookup hosts_meta]"
      ],
      options:[
        { opt:"<field>", desc:"Field to join on (must exist in both datasets)" },
        { opt:"type=inner", desc:"Only matching rows (default)" },
        { opt:"type=left", desc:"All left rows; null-fill where no match" },
        { opt:"type=outer", desc:"All rows from both sides" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> <code>join type=inner</code> is the default — only matching records." },
        { text:"<strong>EXAM TIP:</strong> Prefer <code>lookup</code> over <code>join</code> for performance. Join is a last resort." }
      ],
      presets:[
        { label:"inner join user",  query:"index=web_logs\n| stats count by user\n| join user [| inputlookup usertogroup]" },
        { label:"left join host",   query:"index=web_logs\n| join type=left host [| inputlookup hosts_meta]" }
      ],
      defaultQuery:"index=web_logs\n| stats count by user\n| join user [| inputlookup usertogroup]"
    },

    inputlookup: {
      name:"inputlookup", icon:"⬇", category:"Enrichment", tag:"enrich", tagLabel:"ENRICH", tagClass:"tag-enrich",
      subtitle:"Reads all records from a lookup file into results",
      badges:["Enrichment","Lookup","Generating"],
      description:`The <strong>inputlookup</strong> command reads an entire lookup file (CSV or KV Store) as the search result — it is a <em>generating command</em> used at the start of a search (with a leading <code>|</code>). It does not require events from an index. Use it to browse lookup contents, filter them, or join them against other data.`,
      syntax:[
        "| inputlookup usertogroup",
        "| inputlookup hosts_meta",
        "| inputlookup status_codes",
        "| inputlookup usertogroup | where group=admin"
      ],
      options:[
        { opt:"<lookup_name>", desc:"Name of the lookup file or definition" },
        { opt:"where <expr>", desc:"Optional filter applied before returning results" },
        { opt:"max=<N>", desc:"Maximum rows to return" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> <code>inputlookup</code> = READ a lookup file. <code>outputlookup</code> = WRITE results to a lookup file. <code>lookup</code> = ENRICH events with lookup data." },
        { text:"<strong>EXAM TIP:</strong> <code>inputlookup</code> is a generating command — use it with a leading pipe: <code>| inputlookup name</code>." }
      ],
      presets:[
        { label:"inputlookup usertogroup", query:"| inputlookup usertogroup" },
        { label:"inputlookup hosts_meta",  query:"| inputlookup hosts_meta" },
        { label:"inputlookup status_codes",query:"| inputlookup status_codes" }
      ],
      defaultQuery:"| inputlookup usertogroup"
    },

    outputlookup: {
      name:"outputlookup", icon:"⬆", category:"Enrichment", tag:"enrich", tagLabel:"ENRICH", tagClass:"tag-enrich",
      subtitle:"Writes current search results to a lookup file",
      badges:["Enrichment","Lookup","Write"],
      description:`The <strong>outputlookup</strong> command writes the current search results to a CSV lookup file (or KV Store). It is used to create or update dynamic lookup tables from search results. The results are passed through unchanged — events continue through the pipeline. In this simulation, it annotates results with the target filename but does not actually write a file.`,
      syntax:[
        "index=web_logs | stats count by host | outputlookup host_counts.csv",
        "index=web_logs | stats count by user | outputlookup user_activity.csv",
        "index=web_logs | where status>=500 | outputlookup errors.csv"
      ],
      options:[
        { opt:"<filename.csv>", desc:"Target lookup file name to write to" },
        { opt:"append=true", desc:"Append to existing file instead of overwriting" },
        { opt:"createinapp=true", desc:"Create file in current app context" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> <code>outputlookup</code> = WRITE. <code>inputlookup</code> = READ. <code>lookup</code> = ENRICH." },
        { text:"<strong>EXAM TIP:</strong> Results continue through the pipeline after <code>outputlookup</code> — it does not stop processing." }
      ],
      presets:[
        { label:"outputlookup counts",  query:"index=web_logs\n| stats count by host\n| outputlookup host_counts.csv" },
        { label:"outputlookup errors",  query:"index=web_logs\n| where status >= 500\n| outputlookup errors.csv" }
      ],
      defaultQuery:"index=web_logs\n| stats count by host\n| outputlookup host_counts.csv"
    },

    bin: {
      name:"bin", icon:"⊟", category:"Transforming", tag:"transform", tagLabel:"TRANSFORM", tagClass:"tag-transform",
      subtitle:"Groups continuous values into discrete buckets (alias: bucket)",
      badges:["Transforming","Bucketing","Time Grouping"],
      description:`The <strong>bin</strong> command (alias <strong>bucket</strong>) groups continuous numerical or time values into discrete ranges. Most commonly used to bucket <code>_time</code> into intervals for use with <code>stats</code> — an alternative to <code>timechart</code> that produces a table instead of a chart. The <code>span</code> argument controls bucket size.`,
      syntax:[
        "index=web_logs | bin _time span=1h | stats count by _time",
        "index=web_logs | bin _time span=30m | stats count by _time, status",
        "index=web_logs | bin bytes span=10000 | stats count by bytes",
        "index=web_logs | bucket _time span=1h | stats avg(bytes) by _time"
      ],
      options:[
        { opt:"span=<time>", desc:"Bucket size: 1h, 30m, 1d, 5m, etc." },
        { opt:"bins=<N>", desc:"Auto-compute span to produce N buckets" },
        { opt:"as <alias>", desc:"Rename the bucketed field" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> <code>bin</code> = <code>bucket</code> — they are identical commands." },
        { text:"<strong>EXAM TIP:</strong> Use <code>bin _time span=1h | stats count by _time</code> to get a time table without <code>timechart</code>." }
      ],
      presets:[
        { label:"bin _time span=1h",   query:"index=web_logs\n| bin _time span=1h\n| stats count by _time" },
        { label:"bin by time+status",  query:"index=web_logs\n| bin _time span=1h\n| stats count by _time, status" },
        { label:"bin bytes span=50000",query:"index=web_logs\n| bin bytes span=50000\n| stats count by bytes" }
      ],
      defaultQuery:"index=web_logs\n| bin _time span=1h\n| stats count by _time, status"
    },

    eventstats: {
      name:"eventstats", icon:"∫", category:"Transforming", tag:"transform", tagLabel:"TRANSFORM", tagClass:"tag-transform",
      subtitle:"Adds aggregate statistics to each event without collapsing",
      badges:["Transforming","Aggregation","Non-Collapsing"],
      description:`The <strong>eventstats</strong> command works like <code>stats</code> but instead of replacing events with a summary table, it <em>adds the computed statistics as new fields to each original event</em>. The full event set is preserved. This lets you compare each event's values against group averages inline — a key difference from <code>stats</code>.`,
      syntax:[
        "index=web_logs | eventstats avg(bytes) as avg_bytes by host",
        "index=web_logs | eventstats count as total_events",
        "index=web_logs | eventstats sum(bytes) as host_total by host | eval pct = round(bytes/host_total*100,1)"
      ],
      options:[
        { opt:"<fn>(X) as <alias>", desc:"Stat function with alias added to every event" },
        { opt:"by <field>", desc:"Compute stats per group — each event gets its group's stat" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> <code>eventstats</code> preserves original events. <code>stats</code> collapses them." },
        { text:"<strong>EXAM TIP:</strong> Use <code>eventstats</code> when you need to compare each event against its group average." }
      ],
      presets:[
        { label:"eventstats avg by host",   query:"index=web_logs\n| eventstats avg(bytes) as avg_bytes by host\n| table host, uri, bytes, avg_bytes" },
        { label:"eventstats sum by host",   query:"index=web_logs\n| eventstats sum(bytes) as host_total by host\n| eval pct = round(bytes/host_total*100,1)\n| table host, bytes, host_total, pct" },
        { label:"eventstats global count",  query:"index=web_logs\n| eventstats count as total_events\n| table host, uri, status, total_events" }
      ],
      defaultQuery:"index=web_logs\n| eventstats avg(bytes) as avg_bytes by host\n| table host, uri, bytes, avg_bytes"
    },

    streamstats: {
      name:"streamstats", icon:"≋", category:"Transforming", tag:"transform", tagLabel:"TRANSFORM", tagClass:"tag-transform",
      subtitle:"Running statistics that update as each event is processed in order",
      badges:["Transforming","Running Stats","Streaming"],
      description:`The <strong>streamstats</strong> command calculates statistics in a running/sliding window as events are processed in order. Unlike <code>eventstats</code> (which uses all events for each group), <code>streamstats</code> only uses events seen so far. The <code>window</code> argument creates a sliding window. Sort first to ensure meaningful order.`,
      syntax:[
        "index=web_logs | sort _time | streamstats count as event_number by src_ip",
        "index=web_logs | sort _time | streamstats sum(bytes) as running_total",
        "index=web_logs | sort _time | streamstats window=5 avg(bytes) as rolling_avg"
      ],
      options:[
        { opt:"<fn>(X) as <alias>", desc:"Running stat function" },
        { opt:"by <field>", desc:"Reset counter per group" },
        { opt:"window=<N>", desc:"Only use last N events in calculation (sliding window)" },
        { opt:"reset_on_change=true", desc:"Reset when the by-field value changes" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> Always <code>sort _time</code> before <code>streamstats</code> for meaningful running totals." },
        { text:"<strong>EXAM TIP:</strong> <code>window=5</code> = rolling 5-event average. Without window, it accumulates from the start." }
      ],
      presets:[
        { label:"running count by src_ip",  query:"index=web_logs\n| sort _time\n| streamstats count as event_num by src_ip\n| table _time, src_ip, event_num" },
        { label:"rolling avg bytes",        query:"index=web_logs\n| sort _time\n| streamstats window=5 avg(bytes) as rolling_avg\n| table _time, host, bytes, rolling_avg" },
        { label:"running sum bytes",        query:"index=web_logs\n| sort _time\n| streamstats sum(bytes) as running_total\n| table _time, host, bytes, running_total" }
      ],
      defaultQuery:"index=web_logs\n| sort _time\n| streamstats count as event_num by src_ip\n| table _time, src_ip, event_num, bytes"
    },

    appendcols: {
      name:"appendcols", icon:"⊞", category:"Correlating", tag:"correlate", tagLabel:"CORRELATE", tagClass:"tag-correlate",
      subtitle:"Appends subsearch results as new columns by row position",
      badges:["Correlating","Subsearch","Column Append"],
      description:`The <strong>appendcols</strong> command appends the output of a subsearch as new columns to existing results — matching by row position (first row to first row), NOT by a key field. Unlike <code>join</code> (key-based merge) and <code>append</code> (row stacking), <code>appendcols</code> adds columns positionally.`,
      syntax:[
        "index=web_logs | stats count by host | appendcols [| inputlookup hosts_meta]",
        "index=web_logs | stats count as requests by host | appendcols [search index=web_logs status>=400 | stats count as errors by host]"
      ],
      options:[
        { opt:"[ subsearch ]", desc:"Subsearch whose columns are appended by position" },
        { opt:"override=true", desc:"Allow subsearch fields to overwrite existing fields" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> <code>appendcols</code> matches rows by POSITION not by key. Row 1 to row 1, row 2 to row 2." },
        { text:"<strong>EXAM TIP:</strong> <code>append</code> = add rows. <code>appendcols</code> = add columns. <code>join</code> = merge on field." }
      ],
      presets:[
        { label:"appendcols lookup",  query:"index=web_logs\n| stats count by host\n| appendcols [| inputlookup hosts_meta]" }
      ],
      defaultQuery:"index=web_logs\n| stats count by host\n| appendcols [| inputlookup hosts_meta]"
    },

    regex: {
      name:"regex", icon:"◈", category:"Field Extraction", tag:"extract", tagLabel:"EXTRACT", tagClass:"tag-extract",
      subtitle:"Filters results by regex — keeps or removes matching events",
      badges:["Field Extraction","Filtering","Regex"],
      description:`The <strong>regex</strong> command keeps only events where a field matches a regular expression. It is a filter — it does not extract fields (use <code>rex</code> for extraction). Use <code>regex NOT</code> to exclude matching events. If no field is specified, it searches <code>_raw</code>.`,
      syntax:[
        'index=web_logs | regex uri="^/ctf/"',
        'index=web_logs | regex status="^[45]"',
        'index=web_logs | regex NOT uri="^/admin"',
        'index=web_logs | regex src_ip="^10\\."'
      ],
      options:[
        { opt:'field="<pattern>"', desc:"Keep events where field matches regex pattern" },
        { opt:"NOT", desc:"Invert — keep events that do NOT match" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> <code>regex</code> = filter by pattern. <code>rex</code> = extract fields. Different commands." },
        { text:"<strong>EXAM TIP:</strong> Use <code>regex NOT field=pattern</code> to exclude matching events." }
      ],
      presets:[
        { label:'regex uri ctf',      query:'index=web_logs\n| regex uri="^/ctf/"\n| table host, uri, user, status' },
        { label:'regex 4xx/5xx',      query:'index=web_logs\n| regex status="^[45]"\n| table host, uri, status' },
        { label:'regex src_ip 10.*',  query:'index=web_logs\n| regex src_ip="^10\\."\n| table src_ip, host, uri, status' }
      ],
      defaultQuery:'index=web_logs\n| regex status="^[45]"\n| table host, uri, status, user'
    },

    strcat: {
      name:"strcat", icon:"⟨⟩", category:"Field Extraction", tag:"extract", tagLabel:"EXTRACT", tagClass:"tag-extract",
      subtitle:"Concatenates string values into a new field",
      badges:["Field Extraction","String","Concatenation"],
      description:`The <strong>strcat</strong> command concatenates field values and literal strings into a single new field. It is a distributable streaming command — it runs on indexers and is efficient at scale. Unlike using <code>eval field = a . b</code>, <code>strcat</code> is purpose-built for string joining and handles nulls more gracefully.`,
      syntax:[
        'index=web_logs | strcat host ":" uri full_path',
        'index=web_logs | strcat user "@" host user_host',
        'index=web_logs | strcat method " " uri " " status request_summary'
      ],
      options:[
        { opt:"<field|literal> ... <newfield>", desc:"Space-separated input fields/literals; last arg is output field name" },
        { opt:"allrequired=t", desc:"Only produce output if all input fields are non-null" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> <code>strcat</code> is a distributable streaming command — efficient at scale." },
        { text:"<strong>EXAM TIP:</strong> The last argument is always the output field name." }
      ],
      presets:[
        { label:'strcat host:uri',      query:'index=web_logs\n| strcat host ":" uri full_path\n| table host, uri, full_path' },
        { label:'strcat user@host',     query:'index=web_logs\n| strcat user "@" host user_host\n| table user, host, user_host' },
        { label:'strcat request line',  query:'index=web_logs\n| strcat method " " uri " " status request_line\n| table request_line, bytes' }
      ],
      defaultQuery:'index=web_logs\n| strcat host ":" uri full_path\n| table host, uri, full_path, status'
    },

    gauge: {
      name:"gauge", icon:"◉", category:"Transforming", tag:"transform", tagLabel:"TRANSFORM", tagClass:"tag-transform",
      subtitle:"Sets colored threshold ranges for single-value visualization",
      badges:["Transforming","Visualization","Thresholds"],
      description:`The <strong>gauge</strong> command transforms results into a format suitable for gauge/single-value visualization panels. It sets colored threshold bands (green, yellow, orange, red) based on numeric ranges. It does not create the value itself — first use <code>stats count</code> or similar, then pipe to <code>gauge</code> with threshold values.`,
      syntax:[
        "index=web_logs | stats count as total | gauge total 0 100 500 1000",
        "index=web_logs status>=500 | stats count as errors | gauge errors 0 10 50 100",
        "index=web_logs | stats count by host | gauge count 0 5 15 25"
      ],
      options:[
        { opt:"<field> <min> <low> <high> <max>", desc:"Field name followed by 4 threshold values defining green/yellow/orange/red ranges" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> <code>gauge</code> adds threshold color ranges. It does NOT create the value — use <code>stats count</code> first." },
        { text:"<strong>EXAM TIP:</strong> The four numbers define band boundaries: below first = green, below second = yellow, below third = orange, above = red." }
      ],
      presets:[
        { label:"gauge total events",  query:"index=web_logs\n| stats count as total\n| gauge total 0 10 20 27" },
        { label:"gauge by host",       query:"index=web_logs\n| stats count by host\n| gauge count 0 3 5 10" }
      ],
      defaultQuery:"index=web_logs\n| stats count as total\n| gauge total 0 10 20 27"
    },

    xyseries: {
      name:"xyseries", icon:"⊠", category:"Transforming", tag:"transform", tagLabel:"TRANSFORM", tagClass:"tag-transform",
      subtitle:"Converts rows into a chart-ready table format — inverse of untable",
      badges:["Transforming","Reshaping","Visualization"],
      description:`The <strong>xyseries</strong> command converts a three-column dataset (x field, y field, value field) into a pivot table where unique y-values become columns. This is the inverse of <code>untable</code>. Use it to reshape <code>stats</code> output into a format suitable for charting.`,
      syntax:[
        "index=web_logs | stats count by _time, status | xyseries _time status count",
        "index=web_logs | stats sum(bytes) as total by host, method | xyseries host method total"
      ],
      options:[
        { opt:"<x_field> <y_field> <val_field>", desc:"X-axis field, series field, value field — in that order" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> <code>xyseries</code> = rows to columns (pivot). <code>untable</code> = columns to rows (unpivot)." }
      ],
      presets:[
        { label:"xyseries time+status", query:"index=web_logs\n| stats count by _time, status\n| xyseries _time status count" },
        { label:"xyseries host+method", query:"index=web_logs\n| stats sum(bytes) as total by host, method\n| xyseries host method total" }
      ],
      defaultQuery:"index=web_logs\n| stats count by _time, status\n| xyseries _time status count"
    },

    untable: {
      name:"untable", icon:"⊡", category:"Transforming", tag:"transform", tagLabel:"TRANSFORM", tagClass:"tag-transform",
      subtitle:"Converts table columns into rows — inverse of xyseries",
      badges:["Transforming","Reshaping","Unpivot"],
      description:`The <strong>untable</strong> command converts a wide table (many columns) into a long format (three columns: row key, column name, value). It is the inverse of <code>xyseries</code>. Used when you need to normalize a multi-column result into a row-per-metric format.`,
      syntax:[
        "index=web_logs | stats count by _time, status | xyseries _time status count | untable _time field value",
        "index=web_logs | stats avg(bytes) by host | untable host metric value"
      ],
      options:[
        { opt:"<row_field> <col_field> <val_field>", desc:"Output field names: row key, column name, value" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> <code>untable</code> = pivot wide to long (columns to rows). <code>xyseries</code> = pivot long to wide (rows to columns)." }
      ],
      presets:[
        { label:"untable after xyseries",  query:"index=web_logs\n| stats count by _time, status\n| xyseries _time status count\n| untable _time field value" }
      ],
      defaultQuery:"index=web_logs\n| stats count by _time, status\n| xyseries _time status count\n| untable _time field value"
    },

    tstats: {
      name:"tstats", icon:"⚡", category:"Transforming", tag:"transform", tagLabel:"TRANSFORM", tagClass:"tag-transform",
      subtitle:"Fast statistics on accelerated data model tsidx summaries",
      badges:["Transforming","Performance","Data Models","CIM"],
      description:`The <strong>tstats</strong> command provides extremely fast statistics against accelerated data model summaries (tsidx files). It bypasses raw event scanning entirely. Field names must be prefixed with the dataset name (e.g., <code>Authentication.user</code>). Requires an accelerated data model. This is the fastest way to query CIM-normalized data at scale.`,
      syntax:[
        "| tstats count from datamodel=Authentication by Authentication.user",
        "| tstats count, sum(All_Traffic.bytes_out) from datamodel=Network_Traffic by All_Traffic.src_ip",
        "| tstats count from datamodel=Authentication where Authentication.action=failure by Authentication.src"
      ],
      options:[
        { opt:"from datamodel=<name>", desc:"Specifies the accelerated data model to query" },
        { opt:"where <expr>", desc:"Filter applied at the tsidx level — very fast" },
        { opt:"by <dataset.field>", desc:"Group by a field — must use dataset.field prefix" },
        { opt:"prestats=true", desc:"Return results in format suitable for subsequent stats command" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> <code>tstats</code> requires field prefix with dataset name: <code>Authentication.user</code> not just <code>user</code>." },
        { text:"<strong>EXAM TIP:</strong> <code>tstats</code> only works on accelerated data models. CIM acceleration is OFF by default." },
        { text:"<strong>EXAM TIP:</strong> <code>tstats</code> is the fastest way to query at scale — it skips raw event scanning entirely." }
      ],
      presets:[
        { label:"tstats auth count",    query:"| tstats count from datamodel=Authentication by Authentication.user" },
        { label:"tstats failure count", query:"| tstats count from datamodel=Authentication where Authentication.action=failure by Authentication.src" }
      ],
      defaultQuery:"| tstats count from datamodel=Authentication by Authentication.user"
    },

    datamodel: {
      name:"datamodel", icon:"⬡", category:"Transforming", tag:"transform", tagLabel:"TRANSFORM", tagClass:"tag-transform",
      subtitle:"Searches or returns data from a data model dataset",
      badges:["Transforming","Data Models","CIM"],
      description:`The <strong>datamodel</strong> command returns data from a specific dataset within a data model. Syntax: <code>| datamodel &lt;ModelName&gt; &lt;DatasetName&gt; search</code>. The first argument is the data model name, the second is the dataset within it. Results use prefixed field names (e.g., <code>Authentication.user</code>). Data model acceleration must be enabled for performance.`,
      syntax:[
        "| datamodel Authentication Authentication search",
        "| datamodel Authentication Successful_Authentication search",
        "| datamodel Network_Traffic All_Traffic search | stats count by All_Traffic.src_ip"
      ],
      options:[
        { opt:"<ModelName>", desc:"The data model name (e.g., Authentication, Network_Traffic)" },
        { opt:"<DatasetName>", desc:"The dataset within the model (e.g., Successful_Authentication)" },
        { opt:"search", desc:"Required keyword to execute the search" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> Syntax = <code>| datamodel ModelName DatasetName search</code> — first arg = model, second = dataset." },
        { text:"<strong>EXAM TIP:</strong> Private data models cannot be accelerated. Accelerated models cannot be edited." },
        { text:"<strong>EXAM TIP:</strong> CIM acceleration is OFF by default. An admin must explicitly enable it." }
      ],
      presets:[
        { label:"datamodel Authentication",  query:"| datamodel Authentication Authentication search" },
        { label:"datamodel successful auth", query:"| datamodel Authentication Successful_Authentication search" }
      ],
      defaultQuery:"| datamodel Authentication Authentication search"
    },

    makeresults: {
      name:"makeresults", icon:"⊛", category:"Transforming", tag:"transform", tagLabel:"TRANSFORM", tagClass:"tag-transform",
      subtitle:"Generates synthetic events for testing and development",
      badges:["Transforming","Generating","Testing"],
      description:`The <strong>makeresults</strong> command generates a specified number of synthetic events. It is a <em>generating command</em> used at the start of a search with a leading pipe. Most useful for testing SPL logic, building eval examples, and developing dashboards without needing real data.`,
      syntax:[
        "| makeresults count=5",
        "| makeresults count=3 | eval test_field = now()",
        "| makeresults count=10 | eval idx = _time | eval label = if(idx % 2 == 0, \"even\", \"odd\")"
      ],
      options:[
        { opt:"count=<N>", desc:"Number of events to generate (default 1)" },
        { opt:"annotate=true", desc:"Add splunk_server and splunk_server_group fields" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> <code>makeresults</code> is a generating command — always used with a leading pipe." },
        { text:"<strong>EXAM TIP:</strong> Used for testing SPL without needing real indexed data." }
      ],
      presets:[
        { label:"makeresults count=5",    query:"| makeresults count=5" },
        { label:"makeresults with eval",  query:'| makeresults count=5\n| eval test = "hello"\n| eval ts = strftime(now(), "%Y-%m-%d %H:%M")' }
      ],
      defaultQuery:"| makeresults count=5\n| eval label = \"test_event\"\n| eval ts = strftime(now(), \"%Y-%m-%d\")"
    },

    metadata: {
      name:"metadata", icon:"📋", category:"Transforming", tag:"transform", tagLabel:"TRANSFORM", tagClass:"tag-transform",
      subtitle:"Returns metadata about indexes, sources, sourcetypes, or hosts",
      badges:["Transforming","Generating","Index Info"],
      description:`The <strong>metadata</strong> command returns summary metadata about an index — its sources, sourcetypes, or hosts — without scanning events. It is a <em>generating command</em>. Returns fields: <code>totalCount</code>, <code>firstTime</code>, <code>lastTime</code>, <code>recentTime</code>. Useful for quickly surveying what data is in an index.`,
      syntax:[
        "| metadata type=sourcetypes index=web_logs",
        "| metadata type=hosts index=web_logs",
        "| metadata type=sources index=web_logs"
      ],
      options:[
        { opt:"type=sourcetypes", desc:"List all sourcetypes in the index" },
        { opt:"type=hosts", desc:"List all hosts in the index" },
        { opt:"type=sources", desc:"List all sources in the index" },
        { opt:"index=<name>", desc:"Target index to query" }
      ],
      tips:[
        { text:"<strong>EXAM TIP:</strong> <code>metadata</code> is a generating command — use with leading pipe." },
        { text:"<strong>EXAM TIP:</strong> Returns <code>totalCount</code>, <code>firstTime</code>, <code>lastTime</code> — useful for index inventory." }
      ],
      presets:[
        { label:"metadata sourcetypes",  query:"| metadata type=sourcetypes index=web_logs" },
        { label:"metadata hosts",        query:"| metadata type=hosts index=web_logs" },
        { label:"metadata sources",      query:"| metadata type=sources index=web_logs" }
      ],
      defaultQuery:"| metadata type=sourcetypes index=web_logs"
    }

  }
};